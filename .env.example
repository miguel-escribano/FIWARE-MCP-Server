# ============================================
# FIWARE MCP Server Configuration
# ============================================
# This MCP server supports multiple authentication methods:
# - OAuth (OpenStack Keystone) for enterprise FIWARE deployments
# - HTTP Basic Auth for Nginx-protected deployments
# - No authentication for local development

# ============================================
# Authentication Configuration
# ============================================

# Authentication Type
# Options: oauth, basic, none
AUTH_TYPE=oauth

# Context Broker Connection
CB_HOST=localhost
CB_PORT=1026
CB_PROTOCOL=https  # http or https

# Service/Subservice (FIWARE multi-tenancy)
SERVICE=your_service
SUBSERVICE=/your_subservice

# Credentials (FIWARE_ prefix avoids Windows USERNAME env var conflict)
FIWARE_USERNAME=your_username
FIWARE_PASSWORD=your_password

# OAuth-specific (only required if AUTH_TYPE=oauth)
AUTH_HOST=localhost
AUTH_PORT=15001

# Optional: Pre-fetched token (auto-refreshes if expired)
AUTH_TOKEN=

# ============================================
# Example 1: Telefónica IoT Platform (OAuth)
# ============================================
# AUTH_TYPE=oauth
# AUTH_HOST=auth.iotplatform.telefonica.com
# AUTH_PORT=15001
# CB_HOST=cb.iotplatform.telefonica.com
# CB_PORT=10027
# CB_PROTOCOL=https
# FIWARE_USERNAME=your_username
# FIWARE_PASSWORD=your_password
# SERVICE=your_service
# SUBSERVICE=/your_subservice

# ============================================
# Example 2: FIWARE Lab (OAuth)
# ============================================
# AUTH_TYPE=oauth
# AUTH_HOST=cloud.lab.fiware.org
# AUTH_PORT=5000
# CB_HOST=orion.lab.fiware.org
# CB_PORT=1026
# CB_PROTOCOL=https
# FIWARE_USERNAME=your_username
# FIWARE_PASSWORD=your_password
# SERVICE=your_service
# SUBSERVICE=/your_subservice

# ============================================
# Example 3: Linode with Nginx Basic Auth
# ============================================
# AUTH_TYPE=basic
# CB_HOST=172.233.116.40
# CB_PORT=1026
# CB_PROTOCOL=http
# FIWARE_USERNAME=admin
# FIWARE_PASSWORD=your_password
# SERVICE=openiot
# SUBSERVICE=/Sandbox_1

# ============================================
# Example 4: Local FIWARE (OAuth)
# ============================================
# AUTH_TYPE=oauth
# AUTH_HOST=localhost
# AUTH_PORT=15001
# CB_HOST=localhost
# CB_PORT=1026
# CB_PROTOCOL=https
# FIWARE_USERNAME=admin
# FIWARE_PASSWORD=admin
# SERVICE=openiot
# SUBSERVICE=/

# ============================================
# Example 5: Local development (no auth)
# ============================================
# AUTH_TYPE=none
# CB_HOST=localhost
# CB_PORT=1026
# CB_PROTOCOL=http
# SERVICE=openiot
# SUBSERVICE=/

# ============================================
# Notes
# ============================================
# - AUTH_TYPE is required (oauth, basic, or none)
# - CB_PROTOCOL is required (http or https)
# - Use FIWARE_USERNAME/FIWARE_PASSWORD to avoid conflicts with system variables
# - AUTH_HOST and CB_HOST can be domain names or IP addresses
# - SUBSERVICE must start with / (e.g., /your_subservice)
# - OAuth requires AUTH_HOST and AUTH_PORT
# - Basic Auth only requires FIWARE_USERNAME and FIWARE_PASSWORD
